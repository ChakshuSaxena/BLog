<%= form_for([@post, @post.comments.create]) do |f| %>
  <%= f.label :name %><br>
    <%= f.text_field :name %>
    <br>
    <%= f.label :Comment %><br>
    <%= f.text_area :body %>
  <br>
    <%= f.submit "post" %>
<% end %>
