<div class="form">
  <%= form_for([@post, @post.comments.build]) do |f| %>
  <h3>Add a comment:</h3>
  <%= f.label :commenter_name %><br>
    <%= f.text_field :name %>
    <br><br>
    <%= f.label :Content %><br>
    <%= f.text_area :body %>
  <br>
    <%= f.submit "post" %>
<% end %>
</div>

